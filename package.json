{
  "name": "gh-action-pr-throttler",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to throttle open PRs per author based on merged PR count with configurable policy, exclusions, and messaging.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/graphql": "^7.0.2",
    "@octokit/rest": "^22.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.6.3"
  },
  "keywords": [
    "github-action",
    "pull-requests",
    "throttle",
    "graphql"
  ],
  "author": "",
  "license": "ISC"
}
